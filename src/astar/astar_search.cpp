#include "astar_search.h"
#include <queue>
#include <vector>

namespace AstarSearch {
  int get_one() {
    return 1;
  }
}
//void astar_search(int startx, int starty, int goalx, int goaly,
// vector<string> citymap, double diag_cost) {}

// void children(int x, int y, vector<string> citymap) {}
